
"""
from interface import Interface
def checkSensor(on):
  inter = Interface()
  while(on == True):
    inter.read_sensor(18) ##need something 
    ##to read the button press and execute penta, use threading?

"""
